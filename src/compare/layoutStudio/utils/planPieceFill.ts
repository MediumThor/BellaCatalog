/**
 * Uniform plan fill when slab texture is not drawn.
 * Splash, miter strips, and countertop pieces share this shade (no idx-based or role-based tinting).
 */
export const PLAN_PIECE_NO_TEXTURE_FILL = "rgba(120,200,255,0.14)";

/**
 * Trace workspace overlay fill when slab texture is not drawn (matches prior mid-opacity intent).
 */
export const TRACE_PLAN_NO_TEXTURE_FILL = "rgba(72,140,228,0.27)";
